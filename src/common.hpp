/*
    Common functions expected to be used often throughout the codebase
*/

#include <vector>

// Pythonic range functions
std::vector<int> range(const int& start, const int& end);
std::vector<int> range(const int& end);
